version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (Dependabot bumps the pin + comment).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Keep Python dependencies (uv.lock) current.
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
